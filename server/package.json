{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "license": "Mozilla Public License 2.0",
  "scripts": {
    "build": "tsc",
    "compile": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "test:ci": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "typeorm": "0.3.20"
  },
  "devDependencies": {}
}

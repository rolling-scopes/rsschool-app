{
  "private": true,
  "workspaces": [
    "api",
    "client",
    "lambdas/*",
    "tools/rs-checkist"
  ],
  "scripts": {
    "api:build": "yarn workspace api build"
  },
  "dependencies": {
    "serverless": "^1.50.0"
  }
}

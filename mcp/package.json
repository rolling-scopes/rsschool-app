{
  "name": "@rsschool/mcp-server",
  "version": "0.1.0",
  "description": "MCP server that exposes RS School API actions to AI agents",
  "private": false,
  "license": "Mozilla Public License 2.0",
  "type": "module",
  "bin": {
    "rsschool-mcp": "./dist/server.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "compile": "tsc --noEmit",
    "start": "node dist/server.js",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "test:ci": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "~4.1.0"
  }
}
